output.elasticsearch:
    hosts: [
    {% for host in groups['elasticsearch'] %}
        "http://{{ hostvars[host]['ansible_facts']['default_ipv4']['address'] }}:9200",
    {% endfor %}
    ]
setup.kibana:
    host: "http://{{ hostvars['kibana']['ansible_facts']['default_ipv4']['address'] }}:5601"
filebeat.config.modules.path: ${path.config}/modules.d/*.yml
